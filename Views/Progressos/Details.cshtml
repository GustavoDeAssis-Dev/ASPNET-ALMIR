@model CodeBuddies.Models.Progresso
@{ ViewData["Title"] = "Detalhes"; }

<section class="cb-detail-card">
  <div class="cb-detail-head">
    <h1>Detalhes — Progresso</h1>
  </div>
  <dl class="cb-detail-list">
    <div class="cb-detail-row"><dt>ID</dt><dd>@Model.Id</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.AlunoId)</dt><dd>@Html.DisplayFor(model => model.AlunoId)</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.FaseJogoId)</dt><dd>@Html.DisplayFor(model => model.FaseJogoId)</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.Nota)</dt><dd>@Html.DisplayFor(model => model.Nota)</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.Concluido)</dt><dd>@Html.DisplayFor(model => model.Concluido)</dd></div>
  </dl>
  <div class="cb-form-actions">
    <a class="cb-btn cb-btn-primary" asp-action="Edit" asp-route-id="@Model.Id">Editar</a>
    <a class="cb-btn cb-btn-secondary" asp-action="Index">Voltar</a>
  </div>
</section>