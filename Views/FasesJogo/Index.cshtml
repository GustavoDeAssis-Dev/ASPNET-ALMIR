@model IEnumerable<CodeBuddies.Models.FaseJogo>
@{ ViewData["Title"] = "FaseJogo"; }

<section class="cb-page">
  <div class="cb-page-head">
    <div>
      <h1 class="cb-page-title">FaseJogo</h1>
    </div>
    <a class="cb-btn cb-btn-primary" asp-action="Create">Novo</a>
  </div>
  <div class="cb-table-wrap">
    <table class="table cb-table">
      <thead>
        <tr>
          <th>ID</th>
          <th>@Html.DisplayNameFor(model => model.TituloFase)</th>
          <th>@Html.DisplayNameFor(model => model.Descricao)</th>
          <th>@Html.DisplayNameFor(model => model.NivelDificuldade)</th>
          <th>Ações</th>
        </tr>
      </thead>
      <tbody>
@foreach (var item in Model) {
        <tr>
          <td><span class="cb-id">@item.Id</span></td>
          <td>@Html.DisplayFor(modelItem => item.TituloFase)</td>
          <td>@Html.DisplayFor(modelItem => item.Descricao)</td>
          <td>@Html.DisplayFor(modelItem => item.NivelDificuldade)</td>
          <td>
            <div class="cb-actions">
              <a class="cb-link" asp-action="Edit" asp-route-id="@item.Id">Editar</a>
              <a class="cb-link" asp-action="Details" asp-route-id="@item.Id">Detalhes</a>
              <a class="cb-link cb-link-danger" asp-action="Delete" asp-route-id="@item.Id">Excluir</a>
            </div>
          </td>
        </tr>
      }
      </tbody>
    </table>
  </div>
</section>