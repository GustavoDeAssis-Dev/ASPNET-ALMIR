@model CodeBuddies.Models.FaseJogo
@{ ViewData["Title"] = "Excluir"; }

<section class="cb-delete-card">
  <div class="cb-delete-head">
    <h1>Excluir FaseJogo</h1>
  </div>
  <div class="cb-warning">Confirma a exclusão deste registro?</div>
  <dl class="cb-detail-list">
    <div class="cb-detail-row"><dt>ID</dt><dd>@Model.Id</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.TituloFase)</dt><dd>@Html.DisplayFor(model => model.TituloFase)</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.Descricao)</dt><dd>@Html.DisplayFor(model => model.Descricao)</dd></div>
    <div class="cb-detail-row"><dt>@Html.DisplayNameFor(model => model.NivelDificuldade)</dt><dd>@Html.DisplayFor(model => model.NivelDificuldade)</dd></div>
  </dl>
  <form asp-action="Delete" method="post"><input type="hidden" asp-for="Id" />
    <div class="cb-form-actions" style="justify-content:center;">
      <button class="cb-btn cb-btn-danger" type="submit">Excluir</button>
      <a class="cb-btn cb-btn-secondary" asp-action="Index">Cancelar</a>
    </div>
  </form>
</section>